{
  "name": "5oclock",
  "version": "1.0.0",
  "description": "Website showing cities where it's recently past 5pm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "moment-timezone": "^0.5.43",
    "countries-list": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
